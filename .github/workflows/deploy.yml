name: CD

on:
  workflow_run:
    workflows: ['CI']
    types: [completed]
    branches: [main]

env:
  NX_BASE: main # Base branch for affected commands
  NX_NO_CLOUD: true # Disable Nx Cloud for this workflow

jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    environment: production

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Build backend
        run: yarn nx build:sam backendFlashCards

      - name: Create deployment package
        run: |
          cd packages/backendFlashCards
          zip -r flashcards-api.zip dist/ node_modules/ package.json

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v3
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Update Lambda function
        run: |
          cd packages/backendFlashCards
          aws lambda update-function-code \
            --function-name flashcards-api \
            --zip-file fileb://flashcards-api.zip

          aws lambda wait function-updated \
            --function-name flashcards-api

      - name: Verify deployment
        run: |
          aws lambda get-function --function-name flashcards-api --query 'Configuration.LastModified'

      - name: Run database migrations
        run: |
          echo "🔄 Running database migrations..."

          # Set environment variables
          export NODE_ENV=production
          export DB_HOST=${{ secrets.DB_HOST }}
          export DB_PORT=${{ secrets.DB_PORT || '5432' }}
          export DB_NAME=${{ secrets.DB_NAME }}
          export DB_USERNAME=${{ secrets.DB_USERNAME }}
          export DB_PASSWORD=${{ secrets.DB_PASSWORD }}
          export DB_SSL=true

          # Run migrations using Nx
          yarn nx run @thor-commerce/backendFlashCards:db:migrate

          echo "✅ Database migrations completed"
