import { type ReactNode } from 'react';
export const MDEditor = ({ children }: { children: ReactNode }) => (
  <div>{children}</div>
);

export const Markdown = ({ children }: { children: ReactNode }) => (
  <div>{children}</div>
);

export default MDEditor;
